# Backend

FastAPI API, AI agent, Celery background tasks and SQLAlchemy/Alembic data model for the chatbot. Python 3.12.

Tests live in `tests/` and run with `pytest -q` **from this directory**. The root [`CLAUDE.md`](../CLAUDE.md) documents the module map, environment setup and migration workflow.
