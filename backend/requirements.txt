fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.3
pgvector==0.3.4
psycopg2-binary==2.9.9
celery[redis]==5.4.0
redis==5.0.8
httpx==0.27.2
cryptography==44.0.3
bcrypt==4.2.0
python-jose[cryptography]==3.4.0
python-multipart==0.0.20
email-validator==2.2.0
structlog==24.4.0
openai==1.51.0
# SDK oficial de Anthropic para el proveedor Claude (providers/llm/
# anthropic_client.py). Su API NO es compatible con la de OpenAI, así que no se
# puede atender con el cliente de arriba cambiándole la base_url. Se importa
# PEREZOSAMENTE: si se despliega sin reconstruir la imagen, lo único que falla
# es el proveedor Claude (con un mensaje que lo dice); OpenAI y las pasarelas
# compatibles siguen funcionando igual.
anthropic==0.121.0
pypdf==5.9.0
python-docx==1.1.2
markdown==3.7
pandas==2.2.3
openpyxl==3.1.5
tenacity==9.0.0
slowapi==0.1.9
websockets==13.1
pywebpush==2.0.3
# S3-compatible (Cloudflare R2 / Amazon S3 / MinIO) para las copias de
# seguridad cifradas de la BD (services/backups.py). Cliente síncrono: se usa
# desde tareas Celery y desde endpoints admin vía asyncio.to_thread.
boto3==1.35.99
